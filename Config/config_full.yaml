data:
  base_path: ""
  train_set: []
  val_set: []

model:
  audio_encoder:
    type: facebook/hubert-large-ls960-ft
    downsample_method: pool
    downsample_factor: 4
    pooling:
      kernel_size: 8
      stride: 4
  llm_embedding_channels: 3072

audio:
  sampling_rate: 16000
